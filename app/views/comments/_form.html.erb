<%= form_with model: [@article, @article.comments.build] do |f| %>
<div class="mb-2">
    <%= f.text_area :body, class: 'rounded-md w-full' %>
</div>
<div>
    <%= f.submit class: 'btn' %>
</div>
<% end %>