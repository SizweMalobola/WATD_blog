<%= form_with(model: @article, local: true,class: 'sm:container mx-auto bg-gray-100 p-4 rounded-md') do |f| %>
 <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% @article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="pb-4">
 <%= f.label :title, class: 'block uppercase pb-2' %>
 <%= f.text_field :title, class: 'px-2 py-1 bg-transparent rounded-md border-gray-400 focus:outline-dotted' %>
  </div>
  <div>
 <%= f.label :body, class: 'uppercase pb-2' %>
 <%= f.rich_text_area :body, class: 'mb-4 h-80 border-solid rounded-md' %>
  </div>
 <%= f.submit class:'btn' %>
<% end %>